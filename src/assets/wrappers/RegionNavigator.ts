import styled from 'styled-components'

const RegionNavigatorWrapper = styled.div`
  nav {
    background-color: var(--color-primary-2);
    color: var(--color-primary-4);
    
    ul {
      text-transform: capitalize;
    }
  }
`

export default RegionNavigatorWrapper
